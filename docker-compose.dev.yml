services:
  jarvis:
    build:
      context: .
      target: dev
    ports:
      - '8080:8080'
    env_file:
      - .env
    environment:
      - LOG_LEVEL=debug
    volumes:
      - ./:/app
      - jarvis_node_modules:/app/node_modules
    restart: unless-stopped

volumes:
  jarvis_node_modules:
